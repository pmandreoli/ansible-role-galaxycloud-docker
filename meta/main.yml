galaxy_info:
  author: pietro mandreoli
  description:  galaxy docker 
  company: ELIXIR-ITALY

  license: Apache

  min_ansible_version: 2.2.1.0

  galaxy_tags:
    - galaxy docker

dependencies:
  - { role: indigo-dc.docker, docker_config:  { "data-root": "{{ export_dir }}/docker_image", }}
